from django.apps import AppConfig


class TimetableEngineConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'timetable_engine'
    verbose_name = 'Timetable Engine'
